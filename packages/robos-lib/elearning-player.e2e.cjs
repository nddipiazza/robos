'use strict';
// Run against the live Task Planner after it loads the shared player.
const assert=require('node:assert/strict');
async function evaluate(js){const response=await fetch('http://localhost:19134/eval',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({js})});const data=await response.json();if(data.error)throw Error(data.error);return data.result;}
(async()=>{const result=await evaluate(`(()=>{const fixture={'@id':'urn:robos:qa:player-contract','dcterms:title':'Shared player verification','robos:modules':[{title:'First',markdown:'**Formatted** <img src="x" onerror="window.robosQAUnsafe=true">',labSteps:['Read the section'],quiz:[{question:'Which section?',options:['First','Second'],answer:'First'}]},{title:'Second',markdown:'Continue here.'}]};let player=document.createElement('robos-elearning-player');player.hidden=true;document.body.append(player);player.course=fixture;const key=player.storageKey;localStorage.removeItem(key);player.course=fixture;const root=player.shadowRoot;const styled=!!root.querySelector('style[data-robos-ui-styles]')&&getComputedStyle(root.querySelector('.robos-wait-badge')).display==='none'&&getComputedStyle(root.querySelector('.robos-stream-overlay')).display==='none';const sanitized=!!root.querySelector('strong')&&!root.querySelector('[onerror]');root.querySelectorAll('article input[type=checkbox]').forEach(b=>b.click());const radios=root.querySelectorAll('input[type=radio]');radios[1].click();const wrong=player.progress.completed===0;radios[0].click();const correct=player.progress.completed===1;root.querySelector('[data-next]').click();const next=player.index===1;const styledAfterNavigation=!!player.shadowRoot.querySelector('style[data-robos-ui-styles]')&&getComputedStyle(player.shadowRoot.querySelector('.robos-stream-overlay')).display==='none';player.remove();player=document.createElement('robos-elearning-player');player.hidden=true;document.body.append(player);player.course=fixture;const restored=player.progress.completed===1;localStorage.removeItem(key);player.remove();return {styled,styledAfterNavigation,sanitized,wrong,correct,next,restored};})()`);for(const [name,ok] of Object.entries(result))assert.equal(ok,true,name);console.log('Shared player: sanitized Markdown, quiz validation, navigation, and persisted progress passed.');})().catch(e=>{console.error(e);process.exitCode=1;});
