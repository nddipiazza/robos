# Install Dev Machine Dependencies

Audit, verify, and install all host development dependencies for every component of RobOS.

## Input

$ARGUMENTS — Optional flags (`--check`, `--sys`, `--npm`)

## Procedure

Follow instructions in `plugins/robos/skills/install-dev-deps/SKILL.md`.
