---
description: Import company people and groups through the RobOS graph wizard.
---
Use the `import-users` skill. Discover people from the requested Git checkouts and optional Google Cloud projects, apply the requested email-domain allowlist, review current-team candidates and explicit group memberships, and use Graph Explorer's Users and Groups wizard steps to preview and apply the import. Do not bypass the graph proposal workflow.

$ARGUMENTS
