'use strict';
// Shared provider/model picker. Catalog data comes from the RobOS provider service.
class RobosAgentSelector extends HTMLElement {
 connectedCallback(){if(this.shadowRoot)return;const root=this.attachShadow({mode:'open'});root.innerHTML=`<style>:host{display:block;min-width:0;font:12px system-ui;color:#b9c7d4}.fields{display:grid;grid-template-columns:1fr 1.5fr;gap:12px}label{display:flex;flex-direction:column;gap:6px;min-width:0}input,select{box-sizing:border-box;min-width:0;width:100%;background:#0b1118;color:#e2eaf1;border:1px solid #354455;border-radius:6px;padding:9px 10px;font:13px system-ui;color-scheme:dark}.model-heading{display:flex;justify-content:space-between;align-items:center}button{border:0;background:none;color:#8eabbc;cursor:pointer;padding:0 3px}button:disabled{opacity:.5;cursor:wait}:is(button,input,select):focus-visible{outline:2px solid #00bcd4;outline-offset:2px}.hint{font-size:11px;color:#8fa2b3;margin-top:6px}.hint:empty{display:none}</style><div class="fields"><label>AI agent<select aria-label="AI agent"></select></label><label><span class="model-heading">Model<button type="button" title="Refresh available models" aria-label="Refresh available models">↻</button></span><input aria-label="Model" list="models" placeholder="Provider default"><datalist id="models"></datalist></label></div><div class="hint" role="status"></div>`;this.saved={};root.querySelector('select').onchange=()=>this.selectProvider();root.querySelector('input').oninput=()=>{this.saved[this.value.provider]=this.value.model;this.changed();};root.querySelector('button').onclick=()=>this.load({...this.value,refresh:true}).catch(error=>{root.querySelector('.hint').textContent=error.message;});}
 get value(){return {provider:this.shadowRoot?.querySelector('select').value||'',model:this.shadowRoot?.querySelector('input').value.trim()||''};}
 get provider(){return this.providers?.find(p=>p.id===this.value.provider);}
 get ready(){return !!this.provider?.available&&!this.loading;}
 get error(){return this.provider?.error||'';}
 changed(){this.dispatchEvent(new Event('change',{bubbles:true}));}
 selectProvider(){const p=this.provider;if(!p)return;const root=this.shadowRoot;root.querySelector('input').value=this.saved[p.id]??p.model??'';const list=root.querySelector('datalist');list.replaceChildren();for(const model of p.models||[]){const option=document.createElement('option');option.value=model.id;option.label=model.label;list.append(option);}root.querySelector('.hint').textContent=p.error||(p.modelsStale?'Using saved models; refresh is temporarily unavailable.':'');root.querySelector('button').title=p.modelsUpdatedAt?'Refresh models · Last updated '+new Date(p.modelsUpdatedAt).toLocaleString():'Refresh available models';this.changed();}
 async load({providers,provider,model,refresh=false}={}){this.connectedCallback();this.loading=true;this.changed();const button=this.shadowRoot.querySelector('button');button.disabled=true;try{this.providers=providers||await window.robosProviders.list({refresh});const select=this.shadowRoot.querySelector('select');select.replaceChildren();for(const p of this.providers){const o=document.createElement('option');o.value=p.id;o.textContent=p.label;select.append(o);}select.value=provider||this.providers.find(p=>p.available)?.id||this.providers[0]?.id||'';if(model!==undefined)this.saved[select.value]=model;this.selectProvider();}finally{this.loading=false;button.disabled=false;this.changed();}}
}
customElements.define('robos-agent-selector',RobosAgentSelector);
