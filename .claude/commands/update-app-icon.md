# Update a RobOS App Icon

Replace the SVG icon for a RobOS desktop application.

## Input

$ARGUMENTS — `<app-id-or-name>` optionally followed by a description of the desired icon

## Procedure

Follow instructions in `plugins/robos/skills/update-app-icon/SKILL.md`.
