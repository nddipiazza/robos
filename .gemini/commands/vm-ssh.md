# Run Command on RobOS VM

Execute a shell command on the RobOS VM via SSH.

## Input

$ARGUMENTS — The shell command to run on the VM

## Procedure

Follow instructions in `plugins/robos/skills/vm-ssh/SKILL.md`.
