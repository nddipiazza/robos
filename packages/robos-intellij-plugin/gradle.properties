# Path to locally installed JetBrains IDE — avoids downloading a separate SDK
localIdePath=/home/ndipiazza/apps/idea-IC-233.14808.21

# IntelliJ Platform version (used by CI or when localIdePath is absent)
platformVersion=2025.3

# Kotlin JVM target
jvmTarget=21

# Gradle settings
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true

# Plugin metadata
pluginVersion=1.0.0
pluginSinceBuild=233
pluginUntilBuild=
