const {test}=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const os=require('node:os');const path=require('node:path');
test('certificate policy defaults off and persists separately for each organization',()=>{const before=process.env.HOME;process.env.HOME=fs.mkdtempSync(path.join(os.tmpdir(),'robos-review-policy-'));try{const policy=require('./review-policy');assert.equal(policy.get('Hermetiq/repo').requireCompletionCertificate,false);policy.set('Acme/repo',true);assert.equal(policy.get('acme/other').requireCompletionCertificate,true);assert.equal(policy.get('Hermetiq/repo').requireCompletionCertificate,false);policy.set('ACME/repo',false);assert.equal(policy.get('acme/repo').requireCompletionCertificate,false);assert.throws(()=>policy.set('acme/repo','true'),/boolean/);}finally{process.env.HOME=before;}});
