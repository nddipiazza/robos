{
  "name": "mcp-manager",
  "version": "0.1.0",
  "description": "RobOS MCP Server Manager — Discover, Configure, and Test MCP Servers",
  "main": "main.js",
  "scripts": {
    "start": "electron . --no-sandbox --disable-gpu --disable-dev-shm-usage",
    "test": "node --test"
  },
  "dependencies": {
    "electron": "^33.2.1"
  }
}
