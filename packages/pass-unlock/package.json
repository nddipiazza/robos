{
  "name": "robos-pass-unlock",
  "version": "0.0.3",
  "description": "RobOS Pass Unlock — daily GPG passphrase unlock dialog",
  "main": "main.js",
  "scripts": {
    "start": "electron --no-sandbox --disable-gpu --disable-dev-shm-usage ."
  },
  "dependencies": {
    "electron": "^30.0.0"
  }
}
