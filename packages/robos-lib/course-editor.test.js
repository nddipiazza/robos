'use strict';
const test=require('node:test'),assert=require('node:assert/strict'),{validateRevision}=require('./course-editor-main');
test('course updates reject malformed sections and unanswerable quizzes',()=>{assert.throws(()=>validateRevision({modules:[]}),/1–80/);assert.throws(()=>validateRevision({modules:[{title:'Section'}]}),/Markdown/);assert.throws(()=>validateRevision({modules:[{title:'Section',markdown:'Content',quiz:[{question:'Q',options:['A'],answer:'B'}]}]}),/correct answer/);const modules=[{title:'Section',markdown:'Content',quiz:[{question:'Q',options:['A','B'],answer:'B'}]}];assert.deepEqual(validateRevision({modules}),modules);});
test('multi-section courses introduce objectives before testing knowledge',()=>{const intro={title:'Introduction',markdown:'Learning objectives'},lesson={title:'Helm delivery',markdown:'Cloud acceptance precedes Helm.',quiz:[{question:'What comes first?',options:['Cloud acceptance','Helm packaging'],answer:'Cloud acceptance'}]};assert.throws(()=>validateRevision({modules:[{...intro,quiz:lesson.quiz},lesson]}),/opening section/);assert.equal(validateRevision({modules:[intro,lesson]})[1].quiz.length,1);});
