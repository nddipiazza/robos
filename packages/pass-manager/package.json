{
  "name": "robos-pass-manager",
  "version": "0.0.1",
  "description": "RobOS Pass Manager — GUI for GPG-encrypted password store",
  "main": "main.js",
  "scripts": {
    "start": "electron --no-sandbox --disable-gpu --disable-dev-shm-usage ."
  },
  "dependencies": {
    "electron": "^30.0.0"
  }
}
