# Deploy to RobOS VM

Deploy one or more packages from the local repo to the running RobOS VM.

## Input

$ARGUMENTS — Package name(s) to deploy (e.g. `app-launcher`, `icon-manager`, `robos-icons robos-lib`, or `all`)

## Procedure

Follow instructions in `plugins/robos/skills/deploy-to-vm/SKILL.md`.
