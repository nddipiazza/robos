# Run RobOS Containerized Headless E2E Tests

Run RobOS Electron desktop apps in an isolated Docker container powered by Xvfb and Picom/Mutter compositor.

## Input

$ARGUMENTS — Optional test script or flags (`--build`, `-i`, specific test file path).

## Procedure

Follow instructions in `plugins/robos/skills/test-container/SKILL.md`.
