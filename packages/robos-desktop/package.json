{
  "name": "robos-desktop",
  "version": "1.0.0",
  "description": "RobOS Desktop Shell — full-screen taskbar that replaces the GNOME panel",
  "main": "main.js",
  "scripts": {
    "start": "electron . --no-sandbox --disable-gpu --disable-dev-shm-usage"
  },
  "dependencies": {
    "electron": "^28.0.0"
  }
}
